{
  "name": "quiz-backend",
  "version": "1.0.0",
  "scripts": {
    "deploy": "sam build && sam deploy",
    "import-config": "node src/data/import-config.js"
  },
  "dependencies": {
    "aws-lambda": "^1.0.7",
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0"
  }
}